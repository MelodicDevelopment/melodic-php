<h1>Melodic PHP Framework</h1>
<p>A modern PHP 8.2+ framework with CQRS, JWT authentication, dependency injection, and a middleware pipeline. Built for clarity, not magic.</p>

<h2>Architecture</h2>
<p>Melodic follows a layered architecture where each request flows through a predictable chain:</p>

<pre><code>HTTP Request &rarr; Middleware Pipeline &rarr; Router &rarr; Controller &rarr; Service &rarr; Query/Command &rarr; Database</code></pre>

<p>Every layer has a single responsibility. Controllers handle HTTP concerns, services encapsulate business logic, and queries/commands own data access. There are no facades, no mediator buses, and no hidden magic &mdash; just explicit, traceable code.</p>

<h2>Core Principles</h2>
<ul>
    <li><strong>PHP 8.2+</strong> &mdash; enums, readonly properties, constructor promotion, match expressions, and named arguments throughout</li>
    <li><strong>CQRS data access</strong> &mdash; separate Query and Command objects executed through DbContext</li>
    <li><strong>Constructor injection</strong> &mdash; auto-wiring DI container resolves dependencies from type hints</li>
    <li><strong>PSR-15-style middleware</strong> &mdash; composable pipeline for cross-cutting concerns</li>
    <li><strong>Multi-provider auth</strong> &mdash; OIDC, OAuth2, and local username/password with JWT tokens</li>
    <li><strong>MVC + API</strong> &mdash; separate controller base classes for template-rendered views and JSON APIs</li>
    <li><strong>No hidden behavior</strong> &mdash; no facades, no service locators, no global state</li>
</ul>

<h2>Quick Start</h2>
<pre><code>composer require melodic/framework

# Create your entry point
mkdir -p public config
touch public/index.php config/config.json</code></pre>

<p>Then bootstrap your application:</p>

<pre><code>&lt;?php
// public/index.php
require_once __DIR__ . '/../vendor/autoload.php';

use Melodic\Core\Application;

$app = new Application(dirname(__DIR__));
$app-&gt;loadConfig('config/config.json');

$app-&gt;routes(function ($router) {
    $router-&gt;get('/', MyController::class, 'index');
});

$app-&gt;run();</code></pre>

<h2>Documentation</h2>
<p>Choose a topic from the sidebar to get started, or follow the guide in order:</p>

<ol>
    <?php foreach ($viewBag->pages as $slug => $label): ?>
        <li><a href="/docs/<?= $slug ?>"><?= htmlspecialchars($label) ?></a></li>
    <?php endforeach; ?>
</ol>

<h2>Requirements</h2>
<table>
    <tr><th>Dependency</th><th>Version</th><th>Purpose</th></tr>
    <tr><td>PHP</td><td>8.2+</td><td>Language runtime</td></tr>
    <tr><td>PDO extension</td><td>any</td><td>Database access</td></tr>
    <tr><td>firebase/php-jwt</td><td>^6.0</td><td>JWT encoding and decoding</td></tr>
</table>
